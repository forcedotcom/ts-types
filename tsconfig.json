// Generated - Do not modify. Controlled by @salesforce/dev-scripts
{
  "extends": "@salesforce/dev-config/tsconfig-strict",
  "compilerOptions": {
    "outDir": "lib",
    // src has no ambient @types dependencies; scoping this to an empty list keeps the
    // compile from auto-including a consumer's hoisted @types (e.g. a stale @types/glob)
    // when this package is built in-place inside another project's node_modules (external-compile).
    "types": []
  },
  "include": ["./src/**/*.ts"]
}
